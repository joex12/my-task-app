const { project: project0 } = require('./Iron Aesthetics');
const { project: project1 } = require('./Mobile App');

module.exports = {
  projects: [
    { ...project0, name: 'Iron Aesthetics' },
  { ...project1, name: 'Mobile App' }
  ]
};
